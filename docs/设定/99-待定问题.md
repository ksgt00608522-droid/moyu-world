# 99 待定问题清单

> 状态：进行中  
> 用途：把"还没想好"变成一份可以逐条划掉的清单。  
> 用法：定下一条，就把答案写进对应分类文档，然后回来把这条删掉。

已定的内容**不在这里复述**，避免两处内容漂移。看已定项请直接翻对应分类文档。

---

## A. 玩法骨架（最优先，其它全部依赖它）

- [ ] 有没有终点？通关 / 无限循环 / 挑战最高层数
- [ ] 一次游戏时长预期多久？摸鱼五分钟 / 半小时 / 长期养成
- [ ] 战斗是手动还是自动？每回合手动选 / 自动打完只看日志
- [ ] 死亡怎么处理？读档 / 掉金币 / 无惩罚
- [ ] **大厅之外的四扇门通向什么？** 现在只是占位房间，只有一句描述
- [ ] 除了走路、进门和**跟人说话**，玩家还能做什么？拾取 / 搜索 / 使用物品  
  （对话已经做完了，见 `09-NPC与任务.md`）

## B. 战斗

- [ ] 回合制还是实时？
- [ ] 行动顺序怎么定？速度排序 / 固定先后手
- [ ] 命中与暴击怎么算？
- [ ] 有没有蓝耗、冷却或行动点？
- [ ] 逃跑机制要不要？
- [ ] 战斗在哪触发？走进房间就遇敌 / 房间里有敌人格 / 指令主动开战

## C. 数值

（属性、生命灵性、惩罚、合成公式已经定了，见 `04-数值与成长.md`）

- [ ] 伤害公式什么形状？加减法 / 乘法 / 百分比减伤
- [ ] 平衡目标是什么？不卡关 / 必须刷 / 有挑战
- [ ] **六项基础属性各自"管什么"？** 现在只是摆着，谁都不影响
- [ ] 幸运 / 感知这类软属性怎么用？掉落概率 / 暴击 / 发现隐藏事件
- [ ] **生命与灵性的上限被抬高或降回时，当前值怎么走？**  
  现在没有任何东西会改上限，所以先挂着
- [ ] 灵性枯竭 / 灵性耗尽的具体效果是什么？现在挂着但没效果
- [ ] 主动技能用一次要付代价吗？灵性消耗 / 冷却 / 次数
- [ ] 一个角色最多拥有几个技能？技能怎么获得？
- [ ] **属性会不会永久成长？** 现在只有技能和状态能改，基础值永远是 5
- [ ] **状态的 `unit: "time"`（按真实秒数计时）还没有计时器。**
  `js/statuses.js` 的 `tickStep()` 只认 `step`，所以 `time` 的状态挂上就永远不会过期；
  `tools/validate.mjs` 现在**直接拦下**这种素材，免得埋一个从界面上看不出原因的坑。
  要做先想清楚两件事：**关掉游戏的那段时间算不算**、要不要为它开一个定时器
- [x] ~~**状态面板要不要显示剩余量？**~~ **已定**：状态名本身是个按钮，
  点开弹出详情，里面有「描述 / 剩余量 / 影响 / 计时」四块（见 `06-物品与技能.md`）。
  状态块本身仍然**只列名字**，不放剩余量 —— 那块是吃剩余空间的地方，
  每条再挂一行剩余量会把它撑起来。

## D. 内容规模与风格

- [ ] 大概多少只怪？多少个区域？  
  （区域这一层**已经建好**了，见 `05-地图与探索.md`；这里待定的是**要做几个**，  
  现在只有 `hall` 一个）
- [ ] 有没有剧情主线？还是纯刷
- [ ] 文案风格？正经奇幻 / 沙雕吐槽 / 冷幽默
- [ ] 命名风格？西幻音译 / 中式古风 / 现代梗  
  （**现在有东西在等它**：币种占位名叫「金币」，见 `06-物品与技能.md` 的「货币」。
  改名只动 `data/items.js` 里那一行，存档存的是 id `coin`，不用迁移）
- [x] ~~**物品与装备要不要做？做的话分几类（消耗品 / 装备 / 材料）**~~ **已定**：  
  做物品，分**五类**（消耗品 / 装备 / 材料 / 特殊 / 任务），见 `06-物品与技能.md`。  
  背包**不限格数**，物品分**可堆叠 / 唯一**两种。
- [ ] **装备这一层还没做。** 分类留了 `equip`，但**没有装备槽** ——  
  属性明细里的「装备值」恒为 0，物品也没有"穿上"这个动作。  
  要做先想清楚：几个槽位、同一件装备加什么、穿上之后旧的去哪
- [ ] **丢弃功能先禁掉了。** 详情框里没有丢弃按钮。要做先想清楚三件事：  
  丢在哪（脚下还是直接消失）、丢了能不能捡回来、任务类物品怎么办
- [ ] 物品要不要耐久 / 消耗度？现在物品只有一个数量，没有别的状态
- [ ] 掉落表（打怪掉什么）—— 等战斗

## E. 交互与呈现

- [ ] 复古到什么程度？绿屏终端 / 琥珀单色 / 现代暗色
- [ ] 要不要音效？（合成音，不需要素材文件）
- [ ] 日志要不要打字机效果？
- [ ] 地图要不要显示已探索过的其它房间？

## F. 存档

- [ ] 自动存档的频率？现在是每次状态变更就写
- [ ] 要不要存档槽？（现在因为只有一个角色，是单存档）

## G. 分享相关

- [ ] 分享版要不要隐藏"绑定磁盘文件"这类自用功能？
- [ ] 分享版要不要加一句上手引导？

## H. NPC 与任务

（结构已经建好，见 `09-NPC与任务.md`；这里待定的是**玩法**）

- [ ] **可重复任务的角标要不要改成只标"可交"？** 现在交付后立刻回到"可接"，  
  所以那个 NPC 的角标会**一直亮着**。语义上没错（"这人永远有活给你"），  
  但会一直占着一个角标，跟"有正事要办"的提示混在一起
- [x] ~~**任务有没有奖励？给什么（经验 / 金币 / 物品）**~~ **已定**：  
  加 `reward` 字段，可以给**钱**（`money`）和**物品**（`items`），两个都可选、  
  可以同时写，都是正整数。**没有经验**（角色没有等级）。  
  奖励没有 `log` 字段，交付时系统自动生成一行。见 `09-NPC与任务.md` 的「任务奖励」。
- [ ] 任务能不能放弃 / 重接？现在接了就只能做完（或者一直挂着）
- [ ] 一个 NPC 身上同时挂多个任务时，选项排一列够不够？  
  任务再多要不要分组或折叠
- [ ] **一级区「旁边的人」人一多会不会太长？** 现在一人一块  
  （名字 + 类型 · 态度 + 一个说话按钮），旁边站四五个人就会把  
  「地图点位状态」撑出滚动条。要不要改成一人一行、或者收起来
- [ ] **态度分怎么变？** 现在只是展示，没有任何东西会改它
- [ ] 态度分要不要影响别的？价格 / 愿不愿意说话 / 会不会动手
- [ ] 态度阶段的名字和门槛要不要调？现在是 0 / 20 / 40 / 60 / 80 五档
- [ ] NPC 类型还要加哪些？现在只有 普通 / 神秘 / 商人
- [ ] 普通对话要不要按态度分档？现在是每个 NPC 一组台词，跟态度无关
- [ ] 对话要不要"说过就换"（不重复同一句）？现在是纯随机，可能连着抽到同一句
- [ ] 一个 NPC 出现在多处时算不算同一个"人"？现在是同一个 id、同一份态度和进度
- [ ] 同一个房间里同一格要不要允许站两个 NPC？现在校验直接拦下
- [ ] NPC 要不要会走动？（现在的 `at` 是静态位置表）
- [x] ~~**除了 `walk`，任务还需要哪些目标类型？**~~ **已定一部分**：  
  加了 **`collect`**（收集，**现算不累计**，交付时扣掉，见 `09-NPC与任务.md`）。  
  `reach` / `use` 更早就在 `10-事件点.md` 里定了。  
  还剩 **`kill`**（等战斗）和 **`talk`**（跟某个 NPC 说话）没定。
- [ ] 任务链要不要支持分支 / 汇合？现在是一条直线的 `requires`

## I. 事件点

（结构已经建好，见 `10-事件点.md`；这里待定的是**玩法**）

- [ ] **`set` 效果现在不真改数值。** `G.rules.applyEffect` 是个空壳，  
  素材里写的 `"player.hp": "-5"` 只是记在案上。等 A 组拍了板（有没有血量、  
  死亡怎么处理）再把那个函数填上，**素材一行都不用动**。  
  ⚠️ 只有 `set` 一条在等 —— **`give` / `take` / `money` / `buff` 四条都已经接通了**，
  素材里写上就真生效（见 `10-事件点.md` 第三节的「效果分三条路落地」）。
- [ ] **`take` 一条素材都没写。** 现在没有素材需要"取走玩家的东西" ——  
  任务交付扣物品走的是任务自己的逻辑（`collect`），不是素材里的效果。  
  解析和校验已经就位，等有素材需要再写
- [ ] 事件点要不要"用第几次说不同的话"？现在 `onCollide` / `onUse` 是写死的一串，  
  反复触发就是同一句。`times` 已经记着了，要做得加条件语法
- [ ] 事件点要不要能被"用掉一部分"？（比如泉水能喝三口，第四次干了）  
  现在是全有或全无（`once` 只有 true / false）
- [ ] 事件点要不要能改变房间地形？（踩塌一面墙、开出一条路）  
  现在效果只能写记事和数值
- [ ] 事件点要不要能改变 NPC 态度？（现在态度分没有任何东西会改它）
- [ ] 事件点类型还要加哪些？现在只有 泉水 / 可捡物 / 机关
- [ ] `hidden` 的"空框"要不要带点提示（比如问号）？现在就是纯边框
- [ ] 一个事件点出现在多处时算不算同一个？现在是同一个 id、同一份次数
- [ ] 房间描述里要不要提一句"这里有东西"？现在完全不提，  
  全靠玩家看地图
